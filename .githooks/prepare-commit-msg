#!/bin/sh
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
export PYTHONUTF8=1
if command -v python3 >/dev/null 2>&1; then
	exec python3 "$ROOT/scripts/commit_policy.py" prepare-commit-msg "$1"
else
	exec python "$ROOT/scripts/commit_policy.py" prepare-commit-msg "$1"
fi
